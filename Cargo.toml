[package]
name = "nudge-me"
version = "0.1.0"
edition = "2021"

[dependencies]
nix = { version = "0.29", features = ["term", "poll", "signal", "process", "fs"] }
libc = "0.2"
clap = { version = "4", features = ["derive"] }
chrono = "0.4"
vt100 = "0.16.2"
